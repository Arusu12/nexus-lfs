# Run npm install
npm install

# Run npm install electron globally
npm install electron -g

# Run Electron
electron electron.js

# Pause to keep the PowerShell window open
Read-Host -Prompt "Press Enter to exit"
